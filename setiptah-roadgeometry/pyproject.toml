[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "setiptah-roadgeometry"
description = "A small library for road network geometry queries"
dynamic = ["version"]
authors = [
    { name = "Kyle Treleaven", email = "ktreleav@gmail.com" }
]
readme = "README.md"
dependencies = [
    "bintrees",
    "numpy",  # confirm?
    "networkx",
    # Should these be here?
    "matplotlib",  # at least while draw is here
    "scipy",
]

[project.optional-dependencies]
dev = [
    "matplotlib",
    "ipython",
    "notebook",
    "ipywidgets",
    "geopandas",
    "nbconvert",
]
test = [
    "pytest",
    "pytest-coverage",
    "setiptah-roadgeometry-matching",
    "setiptah-roadgeometry-cpp",
    "shapely",
    "geopandas",
]
package = [
    "black",
    "sphinx",
    "sphinx_rtd_theme",
    "sphinx-codeautolink",
    "sphinx-autobuild",
    "nbsphinx",
]
